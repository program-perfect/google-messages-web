# Material 3 Web Expressive migration

This document records the Material 3 Web Expressive migration checkpoint.

The UI direction uses Material Web components for core controls and custom messenger-specific components for chat bubbles, app shell, and conversation behavior.

Local verification:

pnpm install
pnpm lint
pnpm build
pnpm dev
