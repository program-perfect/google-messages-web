/**
 * Eagerly import all @material/web components used in the app.
 * This file is imported once from a client boundary so that
 * the custom-element definitions are registered before first render.
 */

import "@material/web/all.js";
